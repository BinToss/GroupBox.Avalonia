{
  "description": "dev-environment utilities",
  "scripts": {
    "prepare": "husky && dotnet tool restore",
    "format": "dotnet format && npm run xstyler",
    "release": "npx semantic-release",
    "semantic-release": "npx semantic-release",
    "test": "dotnet test",
    "update-readme": "node ./tools/updateReadMe.mjs",
    "xstyler": "node -- ./tools/formatXamlFiles.mjs --ignore"
  },
  "devDependencies": {
    "@halospv3/hce.shared-config": "^3.3.0",
    "@tsconfig/node24": "^24.0.4",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.12.0",
    "npm": ">=10.5.0"
  },
  "engineStrict": true,
  "private": true,
  "type": "module"
}
